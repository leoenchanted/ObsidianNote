

> 本文解释一个 **电影级阴影调色算法**  
> 目标不是“提亮暗部”，而是 **展开暗部层次（Shadow Detail）而不破坏画面能量**

---

## 一、为什么不能「直接提亮阴影」

在多数初级调色中，阴影通常通过：

`color *= factor;`

或：

`color + lift;`

### ❌ 问题

- 画面整体亮度升高
    
- 黑位被抬灰
    
- 对比结构崩坏
    
- 看起来像“网页滤镜”
    

🎬 **电影调色的目标恰恰相反：**

> 黑色依然是黑色，但你能“看到里面有什么”

这就引出了 **Toe（暗部脚趾）控制** 的概念。

---

## 二、什么是 Toe（暗部脚趾）

在电影摄影的 Tone Curve 中：

- **Toe**：暗部压缩区（Shadows）
    
- **Linear**：中间调
    
- **Shoulder**：高光压缩区
    

`亮度  ↑  |        Shoulder  |       ╭──────  |      ╱  |     ╱   Linear  |    ╱  |───╯ Toe  +────────────→ 输入亮度`

👉 **电影级阴影控制 = 调整 Toe 的“形状”，而不是抬亮**

---

## 三、完整 Shader 代码

`vec3 adjustShadows(vec3 color, float amount) {     amount = clamp(amount, 0.0, 1.0);`

### 🔍 1️⃣ amount 限制

- 保证输入安全
    
- 防止 UI / JS 层传错值导致爆炸
    
- 商业 shader 必备防线
    

---

`float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));`

### 🔍 2️⃣ 感知亮度（Perceptual Luminance）

为什么不用 `avg(color)`？

|方法|问题|
|---|---|
|`(r+g+b)/3`|不符合人眼|
|`max(rgb)`|会错判高光|
|**Rec.709**|🎬 电影工业标准|

📌 **绿色占比最高，因为人眼对绿最敏感**

---

`float toeMask = 1.0 - smoothstep(0.02, 0.30, lum);`

### 🔍 3️⃣ Toe 区域遮罩（最关键的一行）

#### 为什么要 mask？

- 阴影 ≠ 全画面
    
- 中间调绝不能动
    
- 高光完全不碰
    

#### 数值含义

|数值|含义|
|---|---|
|`0.02`|纯黑保护区|
|`0.30`|中灰下沿|

👉 **只有 0.02 ~ 0.30 才算“阴影”**

`smoothstep` 的作用：

- 平滑过渡
    
- 防止 banding
    
- 防止调色痕迹
    

---

`float toeStrength = mix(1.0, 0.75, amount * toeMask);`

### 🔍 4️⃣ Toe 强度（不是亮度）

这是整段代码**最重要的思想转折**。

❌ 错误思路：

> “我要让它更亮”

✅ 正确思路：

> “我要改变暗部曲线的形状”

- `1.0` → 不变
    
- `0.75` → 暗部更平缓
    

👉 **这不是 lift，是曲线展开**

---

`float adjustedLum = pow(lum, toeStrength);`

### 🔍 5️⃣ 非线性暗部展开

`pow()` 在这里不是为了亮度，而是：

- 压缩暗部差异
    
- 拉开暗部层次
    
- 不影响中间调
    

🎬 **这是 Toe 控制的数学本质**

---

`float delta = adjustedLum - lum;`

### 🔍 6️⃣ 亮度变化量（能量差）

关键点：

- 我们并不直接用 `adjustedLum`
    
- 只关心“变化了多少”
    

这是 **能量守恒思想** 的基础。

---

`return clamp(color + delta * 0.35, 0.0, 1.0);`

### 🔍 7️⃣ 极低权重补偿（电影感来源）

为什么是 `0.35`？

- `1.0` → 照片修图
    
- `0.6` → 商业广告
    
- `0.35` → 🎬 电影安全区
    
- `0.25` → 极端作者风格
    

👉 **我们只是“打开信息”，不是“点亮灯光”**

---

## 四、为什么这段代码「怎么拉都不炸」

|机制|作用|
|---|---|
|Toe Mask|限定区域|
|非线性曲线|避免线性拉亮|
|无目标亮度|不会整体抬升|
|delta 权重|防止能量失衡|

---

## 五、和常见错误方案对比

|方法|结果|
|---|---|
|阴影 lift|发灰|
|乘亮度|假 HDR|
|pow(lum, 0.5)|脏|
|**Toe Control**|🎬 稳|

---

## 六、你现在这段代码在工业里的位置

这不是：

- Instagram 滤镜
    
- PS 曲线仿制
    

而是：

- 🎥 摄影机 Log → 显示曲线之间的 **Toe 段控制**
    

你已经站在：

> **“电影调色 vs 照片修图” 的分界线上**

---

## 七、什么时候该继续进阶

当你遇到下面情况之一：

- 阴影怎么调都不炸，但不够“摄影机感”
    
- 高光压不下去
    
- 不同曝光素材一致性差
    

👉 那时，下一步是：

> **Linear → Log → 调色 → Tone Map**